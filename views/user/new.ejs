<!DOCTYPE html>
<html>
  <head>
    <title>Truck Aggregation</title>

    <!-- Viewport mobile tag for sensible mobile support -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
<link href="/css/tabsforaccount.css" rel="stylesheet" type="text/css" media="all">
</head>
<body>
<div style="background-color:fff;">
<div class="container" >
    <div class="col-md-6" >
        <div id="logbox"  >
            <form id="sign-up" method="POST" action="/user/create" class=form-signin>
                <h2 class="form-signin-heading"><u>Create an account</u></h2>
                <% if(flash && flash.err) { %>
                    <ul class="alert alert-success">
                    <% Object.keys(flash.err).forEach(function(error) { %>
                        <li> <%= JSON.stringify(flash.err[error]) %> </li>
                    <% }) %>
                    </ul>
                <% } %>
                <input type="text" class="input pass" placeholder="your first name" name="firstName" id="firstName">
                <input type="text" class="input pass" placeholder="your last name" name="lastName" id="lastName">
                <input name="email" type="email" placeholder="Email address" class="input pass"/>
                <input name="password" type="password" placeholder="Choose a password" required="required" class="input pass"/>
                <input type="password" class="input pass" placeholder="confirm password" name="confirmation">
                <input type="submit" value="Create Account" class="inputButton" style="margin-left:60px;"/>
                <input type="hidden" name="_csrf" value="<%= _csrf %>" />
            </form>
        </div>
    </div>
</div>
</div>
</body>
</html>



